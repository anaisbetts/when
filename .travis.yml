cache:
  directories:
    - node_modules
notifications:
  email: false
language: node_js
node_js:
  - 10
install:
  - cd ./packages/when && npm ci && cd ../react && npm ci
script:
  - cd ../when && npm t && cd ../react && npm t
cache:
  directories:
  - "$HOME/.npm"